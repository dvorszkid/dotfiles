# add this to the .bashrc or .zshrc (done automatically by dfm):
# . $HOME/.shellrc.load

####


# Git repository
DOTFILES=$HOME"/.dotfiles"


# Load bashrc.d files
for sh in "$DOTFILES/bashrc.d/"* ; do
	[ -r "$sh" ] && . "$sh"
done
unset sh
